# 屏幕波形每次刷新发生水平漂移——分析任务书

## 一、项目现象

当前权威工程：

F:\Project\stm32G474VETx\TI\G_Periodic_Signal_Analyzer
\projects\g474_full_integration_test

系统使用真实ADC数据，每约3秒自动分析并刷新一次屏幕。

当前现象：

- 每次刷新后，波形会在水平方向向前移动一点；
- 波形形状和幅值基本不变；
- 漂移具有连续性，不像随机乱码；
- 1T和3T模式都需要检查；
- 当前先分析根因，不要直接修改代码或盲目调整刷新周期。

## 二、必须阅读

1. docs/HANDOFF/README.md
2. docs/HANDOFF/05_SOFTWARE_ARCHITECTURE_AND_DATAFLOW.md
3. docs/HANDOFF/06_DEBUG_HISTORY_AND_FROZEN_DECISIONS.md
4. docs/HANDOFF/07_CURRENT_ISSUES_AND_NEXT_ACTIONS.md
5. docs/REAL_ADC_FIRST_INTEGRATION_REVIEW_2026-07-31.md
6. projects/g474_full_integration_test/Core/Src/main.c
7. projects/g474_full_integration_test/Core/Src/analyzer_bridge.c
8. projects/g474_full_integration_test/Core/Src/display.c
9. 对应头文件
10. tools及tests中已有的phase coverage、waveform lab测试工具

## 三、重点判断的问题

请沿着下面链路逐段检查：

ADC采集起点
→ 2048点数据
→ 基频估计/频率细化
→ 相位折叠
→ 256槽周期波形
→ 794点显示插值
→ 淘晶驰水平反向补偿

重点区分以下原因：

1. 每帧DMA缓冲区起始采样点对应的输入相位不同；
2. 信号源时钟和ADC采样时钟不同步；
3. 实际采样率与代码使用的采样率存在偏差；
4. 基频估计每帧有轻微变化；
5. 相位折叠只按频率取模，没有固定零相位锚点；
6. 显示插值或淘晶驰反向补偿造成额外循环移位；
7. 自动刷新状态机是否重复使用了不同时间点的数据。

## 四、需要记录的指标

连续记录至少20帧，每帧保存：

- 帧序号；
- 刷新时间戳；
- 实际刷新间隔；
- 基频初始值；
- 基频细化值；
- 当前使用的采样率；
- 2048点首个样本值；
- 相位槽覆盖率；
- 最大相位空洞；
- 重建波形峰值所在槽号；
- 最终屏幕峰值所在横坐标；
- 与上一帧的循环互相关位移量。

把每帧位移换算为：

- 槽数/帧；
- 像素/帧；
- 周期比例/帧；
- 相位角/帧。

若相邻帧相位变化为 Δφ，刷新间隔为 T，可估算等效频率偏差：

Δf ≈ Δφ / (2πT)

用于判断漂移是否主要来自采样率或频率估计误差。

## 五、刷新周期对比

分别测试：

- 2秒；
- 3秒；
- 5秒。

判断：

- 每次刷新的相位位移是否随刷新间隔成比例；
- 单位时间内的漂移速度是否基本一致；
- 还是无论间隔多少，每次刷新都随机换相位。

如果漂移速度与时间成比例，重点检查时钟和频率误差。

如果每次刷新相位近似随机，重点检查DMA采集起点和缺少相位锚定。

如果256槽波形不动，但794点显示结果移动，问题在显示重采样或发送方向处理。

## 六、预期解决方向

优先考虑给每帧周期波形增加确定的相位锚点，例如：

1. 以基波相位的上升过零点作为周期起点；
2. 以主峰位置作为周期起点；
3. 用循环互相关将新波形与上一帧对齐。

对于含谐波的任意周期信号，优先评估：

“基波相位锚定 + 循环移位”

或者：

“与上一帧做循环互相关”

不得通过改变幅值、滤波或重新生成理想波形来掩盖水平漂移。

## 七、关于刷新周期

当前3秒可以保留作为基线。

刷新周期只影响观察体验：

- 2秒：响应更快，但漂移会更频繁地被看见；
- 3秒：响应速度和稳定性较均衡；
- 4～5秒：跳动次数减少，但测量结果更新较慢。

不要把刷新周期调整当成根因修复。真正目标是让相同周期波形每帧采用相同相位起点。

## 八、交付要求

本轮只交付：

1. 漂移发生在哪一层；
2. 20帧指标记录；
3. 刷新间隔2/3/5秒的对比；
4. 根因证据；
5. 最小修复方案；
6. 推荐的最终刷新周期。

未经确认，暂时不要修改固件、编译或烧录。